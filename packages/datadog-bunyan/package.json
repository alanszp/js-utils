{
  "name": "@alanszp/datadog-bunyan",
  "version": "1.0.2",
  "description": "Alan's bunyan logger for datadog.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "license": "MIT",
  "files": [
    "**/*"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "compile": "rm -rf ./dist && ./node_modules/.bin/tsc",
    "compile-watch": "./node_modules/.bin/tsc -w",
    "build": "yarn run compile",
    "prepack": "yarn run build",
    "postpack": "rm -rf ./dist"
  },
  "peerDependencies": {
    "@alanszp/datadog-client": "^1.0.0"
  },
  "devDependencies": {
    "@alanszp/datadog-client": "^1.0.2",
    "@types/node": "^15.12.3",
    "ts-node": "^10.0.0",
    "tslint": "^5.11.0",
    "typescript": "^4.3.4"
  }
}
